# P = pawn
# T = Rook
# K = Knights
# B = Bishop
# Q = Queen
# W = King

rows, cols = (8, 8)
board = [["." for i in range(cols)] for j in range(rows)]

# while True:
for i in board:
  for j in board:
    print(j)
  print()
